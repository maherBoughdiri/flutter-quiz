import 'package:flutter/material.dart';

class PersonalInformationScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Personal Information'),
      ),
      body: Center(
        child: Text('This is the personal information page.'),
      ),
    );
  }
}

class EducationScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Education'),
      ),
      body: Center(
        child: Text('This is the education page.'),
      ),
    );
  }
}

class ProfessionalExperienceScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Professional Experience'),
      ),
      body: Center(
        child: Text('This is the professional experience page.'),
      ),
    );
  }
}

class ProfessionalAddressScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Professional Address'),
      ),
      body: Center(
        child: Text('This is the professional address page.'),
      ),
    );
  }
}

class SkillsCertificationsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Skills & Certifications'),
      ),
      body: Center(
        child: Text('This is the skills and certifications page.'),
      ),
    );
  }
}

class PortfolioScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Portfolio'),
      ),
      body: Center(
        child: Text('This is the portfolio page.'),
      ),
    );
  }
}
